fastapi>=0.100.0
uvicorn[standard]>=0.23.0
python-multipart>=0.0.6
pymupdf>=1.23.0
numpy>=1.24.0
opencv-python-headless>=4.8.0
Pillow>=10.0.0

# OCR backends (install at least one)
pytesseract>=0.3.10
# tesseract-ocr (system package required for pytesseract)

# Optional OCR backends
# easyocr>=1.7.0
# paddleocr>=2.7.0

# Semantic material search (optional but recommended)
sentence-transformers>=2.2.0
